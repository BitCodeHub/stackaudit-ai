# ============================================
# StackAudit Environment Variables for Render
# Copy these to Render Dashboard → Service → Environment
# ============================================

# ===========================================
# Backend API Environment Variables
# Service: stackaudit-api
# ===========================================

# Core Configuration (Auto-set by Render)
NODE_ENV=production
PORT=3001

# Database (Auto-linked from stackaudit-db)
# DATABASE_URL=<auto-linked>

# Frontend URL (Update after frontend deploys)
FRONTEND_URL=https://stackaudit-frontend.onrender.com

# Anthropic API (REQUIRED - Set manually)
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-xxx

# Stripe (REQUIRED - Set manually)
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_xxx
# Or for production:
# STRIPE_SECRET_KEY=sk_live_xxx

# Stripe Webhook Secret (REQUIRED - Set manually)
# Create webhook at: https://dashboard.stripe.com/webhooks
# Endpoint URL: https://stackaudit-api.onrender.com/api/webhook/stripe
# Copy the signing secret (starts with whsec_)
STRIPE_WEBHOOK_SECRET=whsec_xxx

# ===========================================
# Frontend Environment Variables
# Service: stackaudit-frontend
# ===========================================

# Backend API URL (Auto-linked from stackaudit-api)
# VITE_API_URL=<auto-linked from stackaudit-api>

# ===========================================
# Optional Configuration
# ===========================================

# Logging
# LOG_LEVEL=info

# CORS (if you need custom domains)
# ALLOWED_ORIGINS=https://stackaudit.ai,https://www.stackaudit.ai

# ===========================================
# Notes
# ===========================================
# 
# 1. Variables marked <auto-linked> are set by Render automatically
#    via the render.yaml configuration
# 
# 2. Variables marked REQUIRED must be set manually in the Render dashboard:
#    - Go to Service → Environment tab
#    - Click "Add Environment Variable"
#    - Paste key and value
#    - Save (triggers redeploy)
# 
# 3. Use TEST keys for development/staging
#    Use LIVE keys only for production
# 
# 4. Never commit API keys to git!
#    This file is just a template
# 
# 5. Update FRONTEND_URL after initial deployment:
#    - Deploy frontend first
#    - Copy the frontend URL
#    - Update backend environment variable
#    - Backend will redeploy automatically
# 
# ===========================================
